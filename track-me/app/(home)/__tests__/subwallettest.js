import React from 'react';
import renderer from "react-test-renderer";
import { fireEvent, render } from '@testing-library/react-native';

describe('subwallet screen', () => {
  it('should render', () => {

  })

  it('should navigate to add wallet page when pressing the plus button', () => {

  })

  it('should navigate to edit wallet page when pressing the blue edit button', () => {

  })

  it('should delete and close a wallet when pressing the red delete button', () => { 

  })

  it('should navigate to add money page when pressing on the wallet item', () => {

  })
})