import React from 'react';
import renderer from "react-test-renderer";
import { fireEvent, render } from '@testing-library/react-native';

describe('edit wallet page', () => {
  it('should render', () => {

  })

  it('should return to subwallet page when pressing the left back arrow', () => {

  })

  it('should navigate to subwallet page when all neccessary input fields are appropriately filled when pressing save', () => {

  })
})