import React from 'react';
import renderer from "react-test-renderer";
import { fireEvent, render } from '@testing-library/react-native';

describe('edit transaction page', () => {
  it('should render', () => {

  })

  it('should return to saving book page when pressing the left back arrow', () => {

  })

  it('should navigate to saving book page when all neccessary input fields are appropriately filled when pressing save', () => {

  })

  it('should delete the transaction when pressing delete', () => {

  })
})